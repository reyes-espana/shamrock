name: Test CI

on: 
  push:
    branches: 
     - master
     - 'dev**'

jobs:
  run-tests:
    runs-on: windows-latest
    
    steps: 
      - uses: actions/checkout@v3
      - name: run tests
        run: npm install -g @angular/cli && npm i && ng test

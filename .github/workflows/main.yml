name: Test CI

on: 
  push:
    branches: 
     - master
     - 'dev**'

jobs:
  run-tests:
    runs-on: windows-latest
    
    steps: 
      - uses: actions/checkout@v3
      - name: run tests
        run: ng test
